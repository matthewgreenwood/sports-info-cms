'use strict';

/**
 * schedule-transport router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::schedule-transport.schedule-transport');
