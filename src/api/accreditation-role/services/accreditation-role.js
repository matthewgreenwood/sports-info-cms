'use strict';

/**
 * accreditation-role service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::accreditation-role.accreditation-role');
