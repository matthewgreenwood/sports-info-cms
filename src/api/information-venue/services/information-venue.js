'use strict';

/**
 * information-venue service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::information-venue.information-venue');
