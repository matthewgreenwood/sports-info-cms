'use strict';

/**
 * accommodation-hotel controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::accommodation-hotel.accommodation-hotel');
