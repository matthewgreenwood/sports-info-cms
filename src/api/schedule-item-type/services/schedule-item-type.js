'use strict';

/**
 * schedule-item-type service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::schedule-item-type.schedule-item-type');
